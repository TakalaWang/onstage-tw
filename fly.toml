# Fly.io 自架設定（adapter-node 全功能版，含訂閱 / 抓取 API）。
# 部署：fly launch --no-deploy  →  fly volumes create onstage_data --size 1  →  fly deploy
app = "onstage-tw"
primary_region = "nrt" # 東京，離台灣近

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "3000"
  ONSTAGE_DB = "/data/onstage.db"
  ONSTAGE_SNAPSHOT = "/data/shows.json"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

# SQLite 與快照的持久化磁碟
[mounts]
  source = "onstage_data"
  destination = "/data"
